---
session: 7
dev: eric
slug: main
branch: main
started: 2026-05-13T18:02:43Z
ended: 2026-05-13T21:44:14Z
wall_clock: 3.67
dev_time: 0.5
review_time: 0.58
duration: 0.5
points: 2
status: closed
pr_number: 12
pr_url: https://github.com/mobiustripper42/captains-log/pull/12
pr_opened_at: 2026-05-13T19:17:08Z
transcript: /home/eric/.claude/projects/-home-eric-captains-log/0af41113-244e-438f-a754-7e2220315930.jsonl
---

# Session 7 — main

**Task:** Phase 2.4 — `conversation_state` in SQLite. Picked up after a short detour through a docs-only ngrok cleanup (PR #11).

**Completed:**
- `lib/state.js` rewritten against better-sqlite3 — sync API, `(db, chatId, data)`, upsert via `INSERT … ON CONFLICT(chat_id) DO UPDATE`. Defensive `String(chatId)` at the boundary.
- `lib/purser.js` updated — drops awaits on state calls, threads `db` into `parseAndConfirm` (which previously didn't take it).
- `test/state.test.js` rewritten against `openDb(':memory:')` + `migrate()`. Dropped the filesystem-atomicity test; added upsert-doesn't-double-insert + chatId-string-coercion.
- 40/40 tests green (same count as session 6 — 6 state tests in, 6 out).
- PR #12 opened against main (OPEN at session close — needs merge).
- Live exercise on bee-grace via curl + Telegram Y verified: parsed confirmation reached TG, Y filed the trip, `conversation_state` row showed `{"status":"idle"}` and `trips` row matched the parsed fields. Used `sqlite3` CLI for verification (preferred going forward — see memory).
- Ngrok references removed from CLAUDE.md, README.md, PROJECT_PLAN.md, docs/SPEC.md → PR #11 (merged mid-session).
- Recovered orphan session-4 log update from `claude/plan-captains-log-QToBw` → PR #10 (merged 18:30:45Z).

**In Progress:** Nothing.

**Blocked:** Nothing.

**Next Steps:**
1. **Merge PR #12** (Phase 2.4 implementation) — `gh pr merge 12 --merge --delete-branch`.
2. **Doc drift cleanup from PR #12 review** (all advisory):
   - `README.md`: still describes `state/<phone>.json` + atomic-rename writes in architecture / layout / storage sections. (Plus the broader pre-Telegram-pivot drift flagged in PR #11.)
   - `CLAUDE.md`: same drift in storage line + architecture diagram + layout + storage rules.
   - `docs/DECISIONS.md`: DEC-010 (flat per-captain state JSON) superseded by 2.4. Add "Superseded by DEC-NNN" header or fold into DEC-012 promote.
   - `.gitignore`: `state/*.json` comment is stale ("DEC-010 runtime state"). Retitle or drop after confirming nothing on bee-grace needs it.
3. **Carryovers from 2.3 code review** (own follow-up branch):
   - `lib/crew.js` TOCTOU race — unique-index migration + `INSERT … ON CONFLICT DO NOTHING` + re-SELECT.
   - `lib/purser.js` boat-slug fallback (`?? 'unknown'`) — surface or reject.
4. Promote DEC-012 (SQLite source-of-truth) pending → accepted in `docs/DECISIONS.md`.
5. **Phase 2.5 — Async Sheet sync job** (3 pts) — `lib/sheets.js` with `google-spreadsheet` v4 + service-account auth, reads `trips.findUnsynced()`, appends rows, marks `sheet_synced_at`. Last task in Phase 2.

**Context:**
- Two PRs in one session is unusual. PR #11 (docs/ngrok cleanup) shipped first because of a mid-2.4-planning redirect. PR #12 carries the actual 2.4 implementation. Session frontmatter `pr_*` points at PR #12.
- Did NOT bundle the 2.3 carryovers (crew TOCTOU + boat-slug wart) into 2.4. Listed for a dedicated follow-up branch.
- `state.js` is now 3 thin SQL one-liners. Code review flagged it could fold into purser.js or a future `lib/conversation.js`. Left it standalone — matches `trips.js`/`crew.js` module shape.
- Orphan-branch scan in `/its-alive` Step 0.5 caught a 3-day-old stranded commit from session 4 (post-PR-merge session-log push that never returned to main). Worth knowing the pattern — `claude/<slug>` branches can collect stale commits after their PR merges if subsequent pushes don't reopen a new PR.
- **Real friction this session:** dev started on phone (mobile copy-paste pain), the May-12 service process was still running and caught the live-test curls with old code, and the verification-query rabbit hole burned tokens. Saved a memory: default to `sqlite3` CLI over better-sqlite3 node one-liners for any ad-hoc DB query in test plans / debug commands. Future fix candidate: a "is the running service from this commit?" smoke-test helper, but only if this pattern shows up again.
- Real points: 2 (Phase 2.4). PR #11 (docs) and PR #10 (orphan recovery) aren't pointed.

**Code Review:** Clean bills of health on both commits' core diffs. Five advisories on PR #12, all documentation drift (README/CLAUDE.md/DEC-010/.gitignore) caused by the storage pivot — none blocking. Captured in Next Steps #2.
