---
session: 5
dev: eric
slug: main
branch: main
started: 2026-05-11T20:34:57Z
ended: 2026-05-11T20:57:42Z
duration: 0.42
points: 3
status: closed
transcript: /home/eric/.claude/projects/-home-eric-captains-log/d2e01359-c77c-4a51-aea2-b1d57a06b1d0.jsonl
---

# Session 5 — main

**Task:** Phase 2.2 — SQLite schema, driver, and startup migration

**Completed:**
- 8-table schema landed in `lib/migrations/001_init.sql` (V1: conversation_state, trips, drills, feedback; V2-baked: crew, trip_crew, drill_crew, handoffs)
- `lib/db.js` — better-sqlite3 driver with WAL + FK + synchronous=NORMAL PRAGMAs
- `lib/migrate.js` — user_version-gated, single-tx-per-file migration runner
- `bin/server.js` runs `migrate()` before listen; `data/` gitignored
- 7 new tests in `test/db.test.js` (table presence, idempotent migrate, FK enforcement, status CHECK, JSON validation, ON DELETE CASCADE); full suite 25/25 green
- PROJECT_PLAN.md row marked ✓, corrected 7→8 tables
- PR #6 merged into main

**In Progress:** Nothing.

**Blocked:** Nothing.

**Next Steps:**
- Phase 2.3 — Trip CRUD + Purser cutover + crew lookup (5 pts). Before starting, settle three carryover decisions from 2.2 code review:
  - `ON DELETE` policy for crew refs — `RESTRICT` vs soft-delete via `crew.active`
  - `updated_at` policy — app-side everywhere vs `AFTER UPDATE` triggers
  - Add `CAPTAINSLOG_DB_PATH` env override in `lib/db.js` for Phase 4 prod/staging separation
- Also: `photo_urls TEXT` needs either a `json_valid` CHECK or escalation to a `trip_photos` table.

**Context:**
- DEC-012 still **pending** in `docs/DECISIONS.md` — promote once 2.3 ships and the storage pivot is irrevocable.
- Schema was hand-designed by Spink in this session and pasted into chat; PROJECT_PLAN.md's "designed in planning conversation" line was aspirational at session start.
- `raw_message_id` was dropped from trips/drills — plaintext logs have no stable row IDs; `parse_json` + `created_at` is the audit trail.
- PR #6: https://github.com/mobiustripper42/captains-log/pull/6

**Code Review:** Clean — no blockers. 7 advisory findings, all deferred to Phase 2.3 (see PR body).
