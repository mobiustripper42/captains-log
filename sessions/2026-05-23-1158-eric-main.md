---
session: 13
dev: eric
slug: main
branch: main
started: 2026-05-23T11:58:46Z
ended:
points:
pr_numbers: [43]
status: open
transcript: /home/eric/.claude/projects/-home-eric-captains-log/7e2a69b4-f356-467f-805b-a1499285e42e.jsonl
---

# Session 13 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 3.4a — Drill structural: `lib/drills.js` + Purser drill confirm flow (issue #33)

**Completed:**
- `lib/parse.js`: added `drill_type` (enum: `abandon_ship | mob | fire | crew_emergency | null`) and `crew_present` (string array | null) to the Haiku JSON schema. Field descriptions only — prompt tuning is 3.4b.
- `lib/drills.js`: new module mirroring `trips.js`. `create()` + `findById()`. `DRILL_TYPES` constant exported so the purser can validate against the schema's CHECK enum without duplicating the list.
- `lib/crew.js`: exports `canonicalNameFor(rawName)` — canonical-name lookup without DB side effect. Used by `drillCrewText` to normalize each name in `crew_present` before joining into `crew_present_text`. Unknown names pass through verbatim.
- `lib/purser.js`:
  - `formatDrillConfirmation(captainName, parsed)` — drill confirmation card with type label, boat, crew, notes. Warns on missing or out-of-enum `drill_type`.
  - `fileDrill({ db, chatId, captain, state, source })` — inserts a `drills` row from `awaiting_confirmation` state. Throws `UNKNOWN_BOAT` or `DRILL_TYPE_MISSING` for friendly nudges.
  - `routeParsed` `intent === 'drill'` branch replaces the 3.4 stub. Validates boat up front via `boatSlugFor` (drills' `boat_slug` is NOT NULL), saves state with `kind: 'drill'`, returns the formatted confirmation reply.
  - `handle` Y-branch dispatches `fileDrill` vs `fileTrip` based on `state.kind` (default `'trip'` for backwards-compat with any in-flight pre-3.4a confirmations).
- After @code-review: added `VALID_DRILL_TYPES` set check in `formatDrillConfirmation` and `fileDrill`. Folds out-of-enum values (Haiku hallucinations like `"MOB"`, `"manoverboard"`) into the existing `DRILL_TYPE_MISSING` throw — captain sees the helpful nudge instead of a generic SQLite CHECK error.
- Tests: +11 in `test/purser.test.js` (formatDrill happy/missing/per-type/out-of-enum, drill route → state, drill unknown-boat, fileDrill happy/unknown-boat/missing-type/out-of-enum/null-crew/unknown-crew, trip default-kind backwards-compat). 100 → 111 green.

**Code review:** Two related correctness gaps around `drill_type` enum validation (Haiku output trusted blindly between parse and insert) — fixed in second commit. Three advisories deferred: (a) drill UNKNOWN_BOAT doesn't preserve state for correction-style re-parse the way the trip path does — accepted as V1 behavior since there's no prior drill state to preserve; (b) `canonicalNameFor` re-reads rosters per call — pre-existing in `resolve`, flag for a future perf pass; (c) no test for `drills.findById` against missing id — minor coverage gap.
**PR:** [#43](https://github.com/mobiustripper42/captains-log/pull/43)
**Points:** 2
**Branch:** task/3.4a-drill-structural
**Opened at:** 2026-05-23T13:08:02Z

**Next Steps:**

**Context:**
