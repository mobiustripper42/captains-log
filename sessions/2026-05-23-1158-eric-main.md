---
session: 13
dev: eric
slug: main
branch: main
started: 2026-05-23T11:58:46Z
ended: 2026-05-23T16:16:06Z
points: 5
pr_numbers: [43, 44]
status: closed
transcript: /home/eric/.claude/projects/-home-eric-captains-log/7e2a69b4-f356-467f-805b-a1499285e42e.jsonl
---

# Session 13 — main

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: Phase 3.4a — Drill structural: `lib/drills.js` + Purser drill confirm flow (issue #33)

**Completed:**
- `lib/parse.js`: added `drill_type` (enum: `abandon_ship | mob | fire | crew_emergency | null`) and `crew_present` (string array | null) to the Haiku JSON schema. Field descriptions only — prompt tuning is 3.4b.
- `lib/drills.js`: new module mirroring `trips.js`. `create()` + `findById()`. `DRILL_TYPES` constant exported so the purser can validate against the schema's CHECK enum without duplicating the list.
- `lib/crew.js`: exports `canonicalNameFor(rawName)` — canonical-name lookup without DB side effect. Used by `drillCrewText` to normalize each name in `crew_present` before joining into `crew_present_text`. Unknown names pass through verbatim.
- `lib/purser.js`:
  - `formatDrillConfirmation(captainName, parsed)` — drill confirmation card with type label, boat, crew, notes. Warns on missing or out-of-enum `drill_type`.
  - `fileDrill({ db, chatId, captain, state, source })` — inserts a `drills` row from `awaiting_confirmation` state. Throws `UNKNOWN_BOAT` or `DRILL_TYPE_MISSING` for friendly nudges.
  - `routeParsed` `intent === 'drill'` branch replaces the 3.4 stub. Validates boat up front via `boatSlugFor` (drills' `boat_slug` is NOT NULL), saves state with `kind: 'drill'`, returns the formatted confirmation reply.
  - `handle` Y-branch dispatches `fileDrill` vs `fileTrip` based on `state.kind` (default `'trip'` for backwards-compat with any in-flight pre-3.4a confirmations).
- After @code-review: added `VALID_DRILL_TYPES` set check in `formatDrillConfirmation` and `fileDrill`. Folds out-of-enum values (Haiku hallucinations like `"MOB"`, `"manoverboard"`) into the existing `DRILL_TYPE_MISSING` throw — captain sees the helpful nudge instead of a generic SQLite CHECK error.
- Tests: +11 in `test/purser.test.js` (formatDrill happy/missing/per-type/out-of-enum, drill route → state, drill unknown-boat, fileDrill happy/unknown-boat/missing-type/out-of-enum/null-crew/unknown-crew, trip default-kind backwards-compat). 100 → 111 green.

**Code review:** Two related correctness gaps around `drill_type` enum validation (Haiku output trusted blindly between parse and insert) — fixed in second commit. Three advisories deferred: (a) drill UNKNOWN_BOAT doesn't preserve state for correction-style re-parse the way the trip path does — accepted as V1 behavior since there's no prior drill state to preserve; (b) `canonicalNameFor` re-reads rosters per call — pre-existing in `resolve`, flag for a future perf pass; (c) no test for `drills.findById` against missing id — minor coverage gap.
**PR:** [#43](https://github.com/mobiustripper42/captains-log/pull/43)
**Points:** 2
**Branch:** task/3.4a-drill-structural
**Opened at:** 2026-05-23T13:08:02Z

## Task 2: Phase 3.5 — /feedback + /file slash commands (issue #35)

**Completed:**
- `lib/feedback.js` (new): CRUD on the `feedback` table (`createPending`, `findLatestPending`, `markFiled`, `markCancelled`, `findById`); `draftFeedback(text)` via Haiku 4.5 with a cached system prompt; `fileViaGh({title, body})` via argv-form `execFile('gh', [...])` with a 15s timeout and regex URL extraction. Env escapes: `CAPTAINSLOG_NO_HAIKU=1` returns a deterministic fake draft, `CAPTAINSLOG_NO_GH_FILE=1` returns a fake URL. `CAPTAINSLOG_FEEDBACK_REPO` overrides the target repo (default `mobiustripper42/captains-log`). `drafted_issue_body` stores `"<title>\n\n<body>"` joined — schema-light, no extra column.
- `lib/purser.js`:
  - `FEEDBACK_PATTERN` (with `/s` flag so multi-line observations survive) and `FILE_PATTERN` — sub-patterns under the existing `SLASH_COMMAND_PATTERN` gate. Both tolerate Telegram's `@botname` suffix.
  - `handleSlash({ db, chatId, captain, body })`: owner-only via `captain.role === 'owner'`. Routes `/feedback <text>`, `/feedback cancel`, `/file`. Empty body / no-pending paths return friendly nudges.
  - `handle()` slash bypass now dispatches to `handleSlash`; in-flight `awaiting_confirmation` state is left untouched (regression test added).
- After @code-review: (a) `draftFeedback` now uses `response.content.find(b => b.type === 'text')` instead of `[0]?.text` — guards against future block types leading; (b) `fileViaGh` extracts the URL via regex match against stdout instead of last-token split — robust against `gh` preambles; (c) added `{ timeout: 15_000 }` to the `execFile('gh', ...)` call; (d) generalized owner-gate reply to "Slash commands are owner-only" (previously read "Feedback is owner-only" even for `/file`); (e) tightened `/file` empty-draft guard to check `pending.drafted_issue_body` directly. Reviewer explicitly verified argv-form `execFile` is safe against argument injection.
- `test/feedback.test.js` (new): 8 tests — DB CRUD, latest-pending scoping by chat_id and status, fake-Haiku and fake-gh paths.
- `test/purser.test.js`: +11 tests — pattern matching, owner gate, draft / file / cancel happy paths, empty / no-pending edges, `awaiting_confirmation` regression.
- `README.md`: note about `gh` CLI dependency and `CAPTAINSLOG_FEEDBACK_REPO` env override.

**Code review:** 2 real bugs (content-block indexing + URL parsing) and 3 cleanup items (execFile timeout, owner-gate phrasing, empty-body guard) all fixed in second commit. 1 advisory deferred: double-`/file` race on the pending row (findLatestPending + markFiled not transactional) — owner-only V1, low probability, accepted.
**PR:** [#44](https://github.com/mobiustripper42/captains-log/pull/44)
**Points:** 3
**Branch:** task/3.5-slash-commands
**Opened at:** 2026-05-23T15:02:02Z

**Next Steps:**
1. Both `b` prompt-tuning tasks — #31 (3.1b open-trip, 5 pts) + #34 (3.4b drill, 3 pts). Both need live captain text on mill-dev. Eric needs uninterrupted computer time to fire test messages and iterate — probably next week. These can run in the same live-exercise session since the dispatch + structural slots for both are already wired and tested.
2. After the `b` tasks land, Phase 3 closes (#29 + #30 already shipped, all structural work done). Phase 4 picks up with 4.1 nightly digest (3 pts, mock-testable build-and-ship) + 4.2 E2E live exercise (3 pts).
3. Phase 3.5 carryover: the double-`/file` race on the pending row (findLatestPending + markFiled not transactional) was flagged in @code-review and deferred as V1 acceptable. If it ever surfaces in practice, wrap the pair in a `tx()`.
4. Issue hygiene: shipped issues don't auto-close because PR bodies don't carry `Closes #N`. Closed #33 and #35 manually post-merge this session. Either fix the PR template or accept the manual close as part of `/kill-this` / `/its-dead` flow.

**Context:**
- `state.kind` field on `conversation_state` is the trip-vs-drill dispatch discriminator. Default is `'trip'` (backwards-compat). 3.1b multi-open arbitration will need to consider whether it also touches drill state, but the schemas don't interact today.
- `feedback` table's `drafted_issue_body` is a single column holding `"title\n\nbody"` joined — chosen over adding a separate `drafted_title` column. Splitting on the first `\n\n` works even when the body itself contains `\n\n` (uses `rest.join('\n\n')`).
- Owner gate for `/feedback` and `/file` is `captain.role === 'owner'` from `config/captains.json`. Currently only Eric. Adding new captains without `role` set defaults to non-owner (default-deny).
- `gh` CLI is now a runtime dependency for `/file`. Documented in README. VPS deploy in Phase 5 will need `gh` installed and authenticated.
- Stray Session 12 (empty, 11:42 UTC today) was marked `abandoned` at `/its-alive` — probably a window opened and immediately abandoned without `/its-dead`. If that pattern recurs, consider a guard.
