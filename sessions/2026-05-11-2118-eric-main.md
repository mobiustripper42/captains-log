---
session: 6
dev: eric
slug: main
branch: main
started: 2026-05-11T21:18:31Z
ended: 2026-05-12T01:48:23Z
duration: 4.5
points: 5
status: closed
transcript: /home/eric/.claude/projects/-home-eric-captains-log/55524e84-1690-4cdd-9af4-c6e422623e6c.jsonl
---

# Session 6 — main

**Task:** Phase 2.3 — Trip CRUD + Purser SQLite cutover + crew lookup (5 pts)

**Completed:**
- `lib/trips.js` — 6 methods (create/findById/findUnsynced/markSynced/update/remove), app-side updated_at, COLUMNS whitelist
- `lib/crew.js` — case-insensitive + alias resolver against `config/crew.json`, lazy-creates crew rows
- `lib/rosters.js` — extracted shared loader from parse.js, added boatSlugFor/routeSlugFor
- `config/crew.json` — initial registry (Eric only)
- `lib/migrations/002_photo_urls_check.sql` — table-rebuild adds `CHECK(photo_urls IS NULL OR json_valid(photo_urls))`
- `lib/migrate.js` — wraps loop in `foreign_keys=OFF` + `foreign_key_check` before COMMIT
- `lib/db.js` — `CAPTAINSLOG_DB_PATH` env override (verified live)
- `lib/purser.js` — Y-reply does `tx(db, () => trips.create(…))`; resolves first_mate→crew_id; maps boat/route→slug; drops `appendStructured` + `filed_to_sheet` artifact
- `bin/server.js` — opens db once at startup, threads into `purserHandle({db})`
- Deleted `lib/structured-log.js`
- 40/40 tests green (was 25): +8 trips, +5 crew, +2 db migration-002 cases
- PR #7 merged into main

**In Progress:** Nothing.

**Blocked:** Nothing.

**Next Steps:**
- Phase 2.4 — `conversation_state` in SQLite (2 pts). Rewrite `lib/state.js` against better-sqlite3 (same interface), rewrite `test/state.test.js` against `:memory:`. Existing `state/*.json` ignored on first boot.
- Before/during 2.4, address two carryover items from 2.3 code review:
  - `lib/crew.js` TOCTOU race — follow-up migration adding `CREATE UNIQUE INDEX crew_name_nocase ON crew(name COLLATE NOCASE)`, then `INSERT … ON CONFLICT DO NOTHING` and re-SELECT. Plain `INSERT OR IGNORE` won't help without the unique index.
  - `lib/purser.js` boat slug fallback — `boat_slug ?? 'unknown'` silently swallows a Haiku miss. Either reject the file and ask the captain, or surface "Filed as unknown boat" in the reply.
- Promote DEC-012 (SQLite source-of-truth) from pending → accepted in `docs/DECISIONS.md` — the cutover landed, it's no longer hypothetical.

**Context:**
- DEC-012 is now load-bearing — `filed_to_sheet: false` artifact is gone, no JSON-file trip log exists.
- Three carryover decisions from session 5 all settled "recommended": crew FK = RESTRICT + `crew.active`, updated_at = app-side, env override = yes. Photo_urls = json_valid CHECK via migration 002.
- DB plumbing is pass-through (server.js opens, threads `db` into `purserHandle`) rather than singleton — explicit + testable.
- Migration 002 used the SQLite table-rebuild dance (DROP/RENAME). Future schema changes that touch `trips` will need to repeat the column copy — keep migrations append-only.
- `boat_slug='unknown'` sentinel is a known wart; will surface as a real captain types.
- Live exercise on bee-grace verified end-to-end: "mate eric" + "mate stoffer" both resolve to `crew_id=1` via alias hit; no duplicate row; full_name persisted from crew.json; `first_mate_text` keeps the raw Haiku capture per design.
- Test plan in PR #7 had two gotchas worth surfacing: (a) curl needs `TELEGRAM_SKIP_SECRET=1` per CLAUDE.md; (b) `better-sqlite3` doesn't ship the `sqlite3` CLI — use a Node one-liner or `apt install sqlite3`.

**Code Review:** Clean — no blockers. 2 advisory + 2 nits, all rolling into Phase 2.4 (see Next Steps + PR #7 body).
