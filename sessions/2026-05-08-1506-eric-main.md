---
session: 2
dev: eric
slug: main
branch: main
started: 2026-05-08T15:06:56Z
ended: 2026-05-08T22:45:59Z
duration: 7.67
points: 2
status: closed
transcript: /home/eric/.claude/projects/-home-eric-captains-log/a425324f-f662-4bd3-bd83-ab80e7d7b063.jsonl
---

# Session 2 — main

**Task:** Switch transport from Twilio SMS to Telegram Bot API; confirm Scribbler working end-to-end on mill-dev.

**Completed:**
- `lib/telegram.js` — new transport: secret-token middleware + `sendMessage` via `fetch`; graceful no-op when `TELEGRAM_BOT_TOKEN` not set
- `bin/server.js` — replaced `/webhook/sms` (Twilio/TwiML) with `/webhook/telegram` (JSON, 200-immediately pattern)
- `lib/scribbler.js` — `{ chatId, body, source }` in; raw log tagged `telegram:<id>`
- `lib/raw-log.js` — `sender` replaces `phone`
- `lib/captains.js` — `id` replaces `phone`, `String(id)` lookup
- `lib/sms.js` — deleted
- `package.json` — `twilio` dep removed
- `config/captains.json` — Eric's Telegram chat_id wired in
- `.env.example`, `CLAUDE.md`, `docs/SPEC.md`, `docs/DECISIONS.md` — updated throughout
- `docs/DECISIONS.md` — DEC-011 placeholder written (full rationale pending paste-in from Eric)
- `raw/2026-05-08.log` — first real captain message in the audit trail
- Scribbler confirmed live: "Got it, Eric. Parsing now." via Telegram on mill-dev

**In Progress:** Nothing.

**Blocked:** DEC-011 full rationale — Eric has the decision notes in another chat, will paste in.

**Next Steps:**
- Phase 2.1: Purser parse — Haiku 4.5 → structured JSON → Telegram confirmation → Y/correction loop (13 pts)
- Cut a `task/2.1-purser-parse` branch at session start next time (didn't do it this session — shipped direct to main)
- Paste DEC-011 rationale into `docs/DECISIONS.md` when found

**Context:**
- mill-dev (Hetzner) is the dev server going forward — has public IPv4 `5.161.209.160` + IPv6; no ngrok/tunnel needed in production
- Tailscale Funnel (`sudo tailscale funnel --bg 3000`) exposes mill-dev's port 3000 publicly for Telegram webhook delivery; run once, persists across reboots
- `setWebhook` only needs to be run once per stable Funnel URL
- Telegram chat_id for Eric: `8637420543` (in `captains.json`)
- Session was shipped directly to `main` — no PR. Branch-per-task next session.

**Code Review:** Skipped — shipping direct to main per session flow.
